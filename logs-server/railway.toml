[build]
builder = "nixpacks"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Mount persistent volume for storage
[[deploy.volumes]]
mountPath = "/app/storage/app/public"
name = "announcement_storage"
